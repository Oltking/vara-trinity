# Session Summary — May 16, 2026

## Goal
Deploy and run VaraAgent's Pulse program on Vara mainnet — an autonomous agent that pulses on-chain, nudges agents, and posts to the Board service.

## Pulse (latest: v3)
- **PID**: `0x51321d7e10b5fa064b6cad675216634336ca2de0e27d0940d184f1548d55f53d`
- **SS58**: `kGhNTAZvuEGbs8SEABeB4uUeWciRERUcNxQ7ERUCrfPFKpk7L`
- **Balance**: 1 VARA (locked) + 3 VARA (unlocked)
- **Code hash**: `0xa7ab0b20049b86199c2de60494eae77336d0762915038eaae86f08dfbe7d81c2`
- **Status**: `StartAuto` called successfully at block 33008329. Next run scheduled with 300-block delay (~15 min)
- **New method**: `withdraw(amount)` added for owner to recover program balance

## Fix applied (reply_deposit)
- **Root cause**: Missing `exec::reply_deposit()` in `run()` — `handle_reply` ran with 0 gas, `msg::load()` panicked
- **Fix**: Added `exec::reply_deposit(msg_id, gas_total * 15 / 100)` after Bridge send. Also `handle_bridge_reply` uses `msg::load()` (Result) instead of `msg::load_bytes()` (panic). Minimum gas check (`gas_total < 500M`) added.
- **Old Pulse (0xc7b3ef…, 31 VARA)**: Receives messages fine but `handle_reply` panics → 31 VARA stuck (no `withdraw` in old code). Cannot recover.

## Deploy history
| Version | PID | Code Hash | Status |
|---------|-----|-----------|--------|
| v1 | `0x6a77...` | `0x1133...` | Init failed (no args). Redeployed. |
| v2 | `0xc7b3ef...` | `0x1133...` | reply_deposit missing → broken handle_reply. 31 VARA stuck. |
| v2b | `0xc64de3...` | `0xa3aa...` | No init args → init panic. Code uploaded, can be reused. |
| v2c | `0x4508cc...` | `0xa3aa...` | Deployed with correct args but code lacks `withdraw`. |
| **v3** | **`0x51321d...`** | **`0xa7ab...`** | **Active. reply_deposit fix + withdraw method.** |

## Known constraints
- **Old Pulse 31 VARA unrecoverable** — no withdraw method, program can't be modified
- **Windows dev**: WSL Ubuntu needed for `cargo build`. MinGW missing `as` assembler.
- **Wallet**: 12.56 VARA remaining in `kGjZUPZLNCN4Ruvwo94o1RcQD6ZyERoZKcwx9YmMsxTPPokXH`
- **Block time**: ~3s/block. 300-block delay = ~15 min between pulses

## Next steps
1. Wait ~15 min for first pulse (block ~33008629+)
2. Verify Bridge query succeeds via `GetStats` → `total_pulses` should increment
3. Set interval shorter via `SetConfig` if needed
